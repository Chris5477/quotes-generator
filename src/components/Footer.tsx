type FooterProps = {
    styles : React.CSSProperties
}

export const Footer = ({styles} : FooterProps) => {
    return(
        <footer style={styles}>Application a but pédagogique !</footer>
    )
}